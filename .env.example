# 复制这个文件为 .env，然后改下面的值

# 访问口令：自己编一个，手机/浏览器第一次打开时要输
ACCESS_TOKEN=改成你自己的口令

# 服务端口
PORT=3000

# 干活的工作目录（Claude 读写文件都在这里面），不填默认用项目下的 workspace 文件夹
# WORKSPACE_DIR=D:\home-workspace

# 工具权限模式：bypassPermissions=全自动干活（默认），acceptEdits=只自动同意文件编辑
# PERMISSION_MODE=bypassPermissions
