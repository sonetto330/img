# 复制这个文件为 .env，然后改下面的值

# 访问口令：自己编一个，手机/浏览器第一次打开时要输
ACCESS_TOKEN=改成你自己的口令

# 服务端口
PORT=3000

# 干活的工作目录（Claude 读写文件都在这里面），不填默认用项目下的 workspace 文件夹
# WORKSPACE_DIR=D:\home-workspace

# 工具权限模式：bypassPermissions=全自动干活（默认），acceptEdits=只自动同意文件编辑
# PERMISSION_MODE=bypassPermissions

# 用哪个模型干活；不填默认 claude-opus-4-7（拍一拍等次要场景也走这个，避免中途被降级）
# CLAUDE_MODEL=claude-opus-4-7

# Claude Code 思考强度：low / medium / high / xhigh / max；不填默认 max
# CLAUDE_EFFORT=max

# claude 可执行文件路径（可选）：引擎默认自动找 npm 全局包里的 claude.exe，
# 找不到（报"引擎进程起不来"）时手动指定
# CLAUDE_CLI_PATH=C:\Users\你\AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\bin\claude.exe

# 长效登录令牌（强烈推荐）：裸 -p 非交互调用不会自动续期订阅登录，令牌约 8 小时
# 过期后麦穗会"突然不回话"。终端里跑一次 claude setup-token（浏览器授权，令牌一年
# 有效），把它给的 token 填在这里，就不再依赖自动续期
# CLAUDE_CODE_OAUTH_TOKEN=在此填 claude setup-token 给的令牌

# 登录心跳（分钟）：没配长效令牌时，定时跑一发 claude -p ping 验登录，掉了就
# Bark 推送提醒。0=关闭；配了 CLAUDE_CODE_OAUTH_TOKEN 会自动停用。默认 360
# LOGIN_HEARTBEAT_MINUTES=360

# 常驻进程闲置多少分钟后回收（释放内存；回收后下次消息付一次冷启动税）。默认 15
# PERSIST_IDLE_MINUTES=30

# 会话自动瘦身阈值：单次上下文（本轮累计输入÷步数）超过这个 token 数，
# 聊完这轮就自动 /compact 把历史压成摘要。0 = 关自动挡，只留会话菜单里的手动"瘦身"
# COMPACT_THRESHOLD_TOKENS=150000

# 一轮超过多少秒没有任何新思考、文字或工具进展就自动停止；最低 30，默认 180 秒
# TURN_STALL_SECONDS=180
# 模型引擎启动后多久仍无首个思考/文字/工具事件就停止；最低 15，默认 45 秒
# FIRST_OUTPUT_TIMEOUT_SECONDS=45

# 提示词缓存改 1 小时档（可选）。订阅通道 CC 本来就自动 1 小时，这行主要给外部 API 通道用：
# 默认 5 分钟档下，隔一阵回消息缓存早过期，整段历史全价重读；开成 1 小时后一小时内回来都吃一折读价。
# 代价是缓存写入从 1.25 倍涨到 2 倍——间歇式聊天划算，连续密聊没差别。
# 注意：中转站不一定透传这个（要带 beta 头），开了之后盯几天控制台 [usage] 的 cache read，没生效就关掉
# ENABLE_PROMPT_CACHING_1H=1

# 外部 API key（可选）：订阅额度不够时的第二通道，按量计费
# 【推荐直接在 app 设置页里填，改完立即生效不用重启】设置页填过的优先生效
# （存 data/settings.json，不进 git）；下面这几行是 .env 兜底写法
# 官方 key 在 platform.claude.com 生成；中转站就填中转站发的 key。只放这里，别写进任何会提交的文件
# EXTERNAL_API_KEY=中转站或官方的key

# 中转站地址（用官方 API 就别填）。填 API 根地址，一般不带 /v1 结尾——
# 中转文档写 https://xxx/v1 的话，这里填 https://xxx 试；不通再按它文档原样填
# EXTERNAL_API_BASE_URL=https://你的中转地址

# 验证方式：默认走 Anthropic 的 x-api-key 头，多数中转兼容；
# 报 401 而 key 确定没错时，多半是中转只认 Bearer，把下面这行打开
# EXTERNAL_API_AUTH=bearer

# 首页天气的位置（经纬度）；不填默认攀枝花。查经纬度可搜"某某城市 经纬度"
# WEATHER_LAT=26.5843
# WEATHER_LON=101.7168

# 梯子代理（没开 TUN 模式就把下面两行前面的 # 去掉，端口改成你梯子的：Clash 一般 7897，v2rayN 一般 10808）
# HTTPS_PROXY=http://127.0.0.1:7897
# HTTP_PROXY=http://127.0.0.1:7897

# Bark 推送（可选）：iPhone 装 Bark App，把 App 里的 key 填进来
# 页面没打开时，麦穗回复完/出错会推一条通知到你手机；不填就整个功能关掉
# BARK_KEY=在此填 Bark 给你的那串 key

# ElevenLabs TTS（可选）：气泡上的 🔊 按钮，用麦穗的声音念回复
# 两个都填才生效，任缺一项前端 🔊 会报错；按字符扣 ElevenLabs 额度
# ELEVENLABS_KEY=在此填 ElevenLabs 后台的 API key
# ELEVENLABS_VOICE=在此填音色的 voice id（ElevenLabs 声音库里选一个）
