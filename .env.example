# 复制这个文件为 .env，然后改下面的值

# 访问口令：自己编一个，手机/浏览器第一次打开时要输
ACCESS_TOKEN=改成你自己的口令

# 服务端口
PORT=3000

# 干活的工作目录（Claude 读写文件都在这里面），不填默认用项目下的 workspace 文件夹
# WORKSPACE_DIR=D:\home-workspace

# 工具权限模式：bypassPermissions=全自动干活（默认），acceptEdits=只自动同意文件编辑
# PERMISSION_MODE=bypassPermissions

# 梯子代理（没开 TUN 模式就把下面两行前面的 # 去掉，端口改成你梯子的：Clash 一般 7890，v2rayN 一般 10808）
# HTTPS_PROXY=http://127.0.0.1:7890
# HTTP_PROXY=http://127.0.0.1:7890

# Bark 推送（可选）：iPhone 装 Bark App，把 App 里的 key 填进来
# 页面没打开时，麦穗回复完/出错会推一条通知到你手机；不填就整个功能关掉
# BARK_KEY=在此填 Bark 给你的那串 key
